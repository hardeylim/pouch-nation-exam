<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="event"
            type="com.example.pouchnationexam.usecase.api.Events" />
    </data>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="24dp">
            <TextView
                android:padding="4dp"
                android:id="@+id/event_title"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:textSize="32sp"
                android:textStyle="bold"
                android:text="@{event.eventTitle}"
                tools:text="Mariah Carey Caution World Tour: Live in Manila"/>

            <ImageView
                android:id="@+id/event_date_icon"
                app:layout_constraintTop_toBottomOf="@+id/event_title"
                app:layout_constraintStart_toStartOf="parent"
                android:padding="16dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_baseline_calendar_today_24"/>

            <TextView
                app:layout_constraintTop_toBottomOf="@id/event_title"
                app:layout_constraintStart_toEndOf="@id/event_date_icon"
                android:paddingLeft="16dp"
                android:paddingTop="16dp"
                android:paddingBottom="8dp"
                android:id="@+id/event_date"
                android:textSize="16sp"
                android:text="@{event.eventDateTime}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="July 13-14 2019"/>

            <TextView
                app:layout_constraintTop_toBottomOf="@id/event_date"
                app:layout_constraintStart_toEndOf="@id/event_date_icon"
                android:id="@+id/event_time"
                android:paddingStart="16sp"
                android:textSize="16sp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="3:00 pm - 3:00 am GMT +8:00"/>


            <ImageView
                android:id="@+id/event_location_icon"
                app:layout_constraintTop_toBottomOf="@+id/event_time"
                app:layout_constraintStart_toStartOf="parent"
                android:padding="16dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_outline_map_24"/>

            <androidx.constraintlayout.widget.Barrier
                android:id="@+id/barrier_upper_half"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:orientation="vertical"
                app:barrierDirection="bottom"
                app:constraint_referenced_ids="event_location_icon, event_location"/>

            <TextView
                android:id="@+id/event_location"
                app:layout_constraintTop_toBottomOf="@id/event_time"
                app:layout_constraintStart_toEndOf="@id/event_location_icon"
                android:padding="16dp"
                android:text="@{event.eventLocation}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                tools:text="Courtyard 66"/>

            <TextView
                app:layout_constraintTop_toBottomOf="@id/barrier_upper_half"
                android:id="@+id/event_description_title"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:textSize="20dp"
                android:textStyle="bold"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:textColor="@color/black"
                android:text="@string/mock_event_title"/>

            <TextView
                app:layout_constraintTop_toBottomOf="@id/event_description_title"
                android:id="@+id/event_description_body"
                android:layout_height="wrap_content"
                android:paddingTop="8dp"
                android:layout_width="match_parent"
                android:textSize="16sp"
                android:textColor="@color/black"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.core.widget.NestedScrollView>
</layout>
